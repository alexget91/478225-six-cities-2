import {sortingOptions} from "./constants";

const Settings = {
  API_TIMEOUT: 5000,
  DEFAULT_SORT: sortingOptions.popular,
  COMMENT_LENGTH_MIN: 50,
  COMMENT_LENGTH_MAX: 300,
};

export default Settings;
